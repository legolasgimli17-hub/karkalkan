'use strict';

/* KârKalkan vNext — evidence-first finance, live order signals and explainable decisions. */
const kkVNext={evidence:{sales:null,returns:null,cost:null,cargo:null},decision:null,liveTimer:null,mounted:false};
const kkClamp=(v,min=0,max=1)=>{const n=Number(v);return Number.isFinite(n)?Math.max(min,Math.min(max,n)):0};
function kkMoney(v,currency='TRY'){const n=Number(v);if(!Number.isFinite(n))return '—';try{return new Intl.NumberFormat('tr-TR',{style:'currency',currency,maximumFractionDigits:0}).format(n)}catch{return `${Math.round(n).toLocaleString('tr-TR')} ${currency}`}}
function kkEsc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function kkDateLabel(v){const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleDateString('tr-TR',{day:'2-digit',month:'short'})}
function kkHasNumber(v){return v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v))}

function kkMount(){
 if(kkVNext.mounted)return;const dashboard=document.getElementById('dashboard'),syncMessage=document.getElementById('syncMessage');if(!dashboard||!syncMessage)return;kkVNext.mounted=true;
 const oldTrend=document.getElementById('trendPanel');if(oldTrend)oldTrend.hidden=true;const oldTruth=document.getElementById('financeTruthPanel');if(oldTruth){const e=oldTruth.querySelector('.eyebrow'),h=oldTruth.querySelector('h3');if(e)e.textContent='HAKEDİŞ VE KESİNTİLER';if(h)h.textContent='Doğrulanmış finans kalemleri'}
 document.querySelector('.v4-topbar .eyebrow')?.replaceChildren(document.createTextNode('KÂRKALKAN · PARA AKIŞI'));const topTitle=document.querySelector('.v4-topbar h1');if(topTitle)topTitle.textContent='Paranın nerede kaldığını gör';const sideStatus=document.querySelector('.side-status small');if(sideStatus)sideStatus.textContent='Hızlı sinyal · doğrulanmış finans';
 const nav=document.querySelector('.side-nav');if(nav){const links=[...nav.querySelectorAll('a')],names=['Genel Bakış','Mağaza Bağlantısı','Güvenli Bağlantı','Ürün Maliyetleri','Veri Geçmişi'];links.forEach((a,i)=>{a.innerHTML=`<span>${String(i+1).padStart(2,'0')}</span> ${names[i]||a.textContent}`});const live=document.createElement('a');live.href='#kkLiveStream';live.innerHTML='<span>06</span> Canlı Siparişler';nav.append(live);const flow=document.createElement('a');flow.href='#kkFlowCard';flow.innerHTML='<span>07</span> Para Akışı';nav.append(flow);const all=[...nav.querySelectorAll('a')];const setActive=()=>{let best=null,bestTop=Infinity;for(const a of all){const target=document.querySelector(a.getAttribute('href'));if(!target)continue;const top=Math.abs(target.getBoundingClientRect().top-120);if(top<bestTop){best=a;bestTop=top}}all.forEach(a=>a.classList.toggle('is-active',a===best))};window.addEventListener('scroll',setActive,{passive:true});setActive()}
 const command=document.createElement('div');command.className='kk-command-bar';command.innerHTML=`<article class="kk-command-main"><div class="kk-kicker">KârKalkan yaklaşımı</div><h3 class="kk-command-title">Hızlı sipariş sinyali, sonradan doğrulanan finans gerçeği.</h3><p class="kk-command-copy">Sipariş geldiğinde erken sinyal al; hakediş, kargo ve maliyet verisi geldikçe rakamın hangi bölümünün gerçekten doğrulandığını ayrı gör. Tek bir “net kâr” rakamına körü körüne güvenmek yerine kanıtı da yanında taşı.</p><div class="kk-command-tags"><span>Satış kanıtı</span><span>İade kanıtı</span><span>Maliyet kapsamı</span><span>Kargo eşleşmesi</span><span>Hakediş düzeltmeleri</span></div></article><article class="kk-live-card"><div class="kk-live-head"><div><strong>Canlı sipariş sinyali</strong><small>Webhook hızlı haberdar eder; periyodik senkron finansı doğrular.</small></div><span id="kkWebhookState" class="kk-live-state"><i></i> Kontrol ediliyor</span></div><div class="kk-live-actions"><button id="kkWebhookBtn" class="btn ghost compact-btn" type="button">Canlı akışı kur</button><span id="kkWebhookMessage" class="kk-live-note">Önce mağaza ve API bilgileri gerekir.</span></div></article>`;syncMessage.after(command);
 const insight=document.createElement('div');insight.className='kk-insight-grid';insight.innerHTML=`<article class="kk-chart-card"><div class="kk-card-head"><div><div class="kk-kicker">Günlük görünüm</div><h3>Satış ve bilinen nakit akışı</h3><p>Satış hacmi ile platform/kargo kesintileri ve stopaj sonrası bilinen nakdi gün gün birlikte oku.</p></div><span class="kk-card-badge">Grafik</span></div><div id="kkDailyChart" class="kk-chart-wrap"><div class="kk-chart-empty">Veri bekleniyor.</div></div><div class="kk-chart-legend"><span class="sales"><i></i>Toplam satış</span><span class="cash"><i></i>Stopaj dahil bilinen nakit</span></div></article><article class="kk-evidence-card"><div class="kk-card-head"><div><div class="kk-kicker">Finansal veri güveni</div><h3 id="kkHealthTitle">Rakamın dayanağı</h3><p id="kkHealthCopy">Puan, mağazanın başarısını değil finans verisinin ne kadar kanıtlı ve eksiksiz olduğunu gösterir.</p></div><span id="kkHealthBadge" class="kk-card-badge">Veri güveni</span></div><div id="kkEvidenceList" class="kk-evidence-list"></div><div class="kk-evidence-callout">KârKalkan emin olmadığı kısmı saklamaz. Ölçülemeyen bileşen skora zorla %100 ya da %0 olarak eklenmez.</div></article>`;const health=document.querySelector('#dashboard .health-grid');(health||command).after(insight);
 const flow=document.createElement('article');flow.id='kkFlowCard';flow.className='kk-flow-card';flow.innerHTML=`<div class="kk-card-head"><div><div class="kk-kicker">Para köprüsü</div><h3>Satıştan bilinen nakde</h3><p>Kesintilerin etkisini tek bakışta gör. Ürün maliyeti bu köprünün ayrı kapsamıdır; bu görünüm muhasebe net kârı değildir.</p></div><span class="kk-card-badge">Açıklanabilir finans</span></div><div id="kkFlowGrid" class="kk-flow-grid"><div class="kk-chart-empty" style="grid-column:1/-1">Veri bekleniyor.</div></div>`;const kpis=document.querySelector('#dashboard .v4-kpis');(kpis||insight).after(flow);
 const radar=document.createElement('article');radar.id='kkLeakRadar';radar.className='kk-live-stream';radar.innerHTML=`<div class="kk-card-head"><div><div class="kk-kicker">Para kaçağı radarı</div><h3>Önce kontrol etmen gereken yerler</h3><p>Kesin zarar iddiası değil; finans sonucunu bozabilecek kanıt boşlukları ve sıra dışı hareketler.</p></div><span class="kk-card-badge">Açıklanabilir</span></div><div id="kkLeakList" class="kk-live-list"><div class="kk-live-empty">Radar için doğrulanmış veri bekleniyor.</div></div>`;flow.after(radar);
 const liveStream=document.createElement('article');liveStream.id='kkLiveStream';liveStream.className='kk-live-stream';liveStream.innerHTML=`<div class="kk-card-head"><div><div class="kk-kicker">Canlı akış</div><h3>Son sipariş sinyalleri</h3><p>Webhook durum değişikliklerini hızlı gösterir; finansal tutar daha sonra hakedişle doğrulanır.</p></div><span class="kk-card-badge">Minimum veri</span></div><div id="kkLiveList" class="kk-live-list"><div class="kk-live-empty">Canlı sipariş akışı henüz kurulmadı.</div></div>`;radar.after(liveStream);document.getElementById('kkWebhookBtn')?.addEventListener('click',kkRegisterWebhook)
}

function kkRenderEvidence(){const host=document.getElementById('kkEvidenceList');if(!host)return;const decision=kkVNext.decision,components=Array.isArray(decision?.components)?decision.components:[];if(components.length){const score=Number(decision?.confidenceScore??decision?.healthScore??0),label=decision?.confidenceLabel||decision?.healthLabel||'Değerlendiriliyor';document.getElementById('kkHealthTitle').textContent=`${score}/100 · ${label}`;document.getElementById('kkHealthBadge').textContent='Veri güveni';host.innerHTML=components.map(c=>{const applicable=c?.applicable!==false&&kkHasNumber(c?.score),p=applicable?Math.max(0,Math.min(100,Number(c.score))):0;return `<div class="kk-evidence-row"><header><span>${kkEsc(c.label)}</span><strong>${applicable?`%${p}`:'Bu dönemde ölçülmedi'}</strong></header><div class="kk-meter"><i style="width:${p}%"></i></div><small>${kkEsc(c.help)}${applicable?` · Temel ağırlık %${Number(c.weight)||0}`:' · Skora katılmadı'}</small></div>`}).join('');return}const rows=[['Satış kanıtı',kkVNext.evidence.sales,'Satış hacminin sipariş satırlarıyla doğrulanabilen bölümü.'],['İade kanıtı',kkVNext.evidence.returns,'Ölçülebilir iadelerin kabul edilmiş claim kayıtlarıyla doğrulanabilen bölümü.'],['Ürün maliyeti kapsamı',kkVNext.evidence.cost,'Maliyeti bilinen satış hacmi.'],['Kargo eşleşmesi',kkVNext.evidence.cargo,'Kargo faturasının siparişten ürüne kanıtlı dağıtılabilen bölümü.']];host.innerHTML=rows.map(([label,value,help])=>{const ready=kkHasNumber(value),p=ready?Math.round(kkClamp(value)*100):0;return `<div class="kk-evidence-row"><header><span>${label}</span><strong>${ready?`%${p}`:'—'}</strong></header><div class="kk-meter"><i style="width:${p}%"></i></div><small>${help}</small></div>`}).join('')}
function kkRenderDaily(data){const host=document.getElementById('kkDailyChart');if(!host)return;const rows=Array.isArray(data?.daily)?data.daily.filter(r=>r?.day):[];if(rows.length<2){host.innerHTML='<div class="kk-chart-empty">Grafik için en az iki günlük veri gerekir.</div>';return}const W=720,H=250,L=42,R=12,T=16,B=34,plotW=W-L-R,plotH=H-T-B,cashOf=r=>Number(r.knownCashAfterFeesAndStoppage??r.knownFeeNet??r.adjustedSellerRevenue??r.sellerRevenue)||0,values=rows.flatMap(r=>[Number(r.grossSales)||0,cashOf(r)]),max=Math.max(1,...values),min=Math.min(0,...values),range=Math.max(1,max-min),x=i=>L+i*plotW/(rows.length-1),y=v=>T+(max-(Number(v)||0))*plotH/range,sales=rows.map((r,i)=>`${x(i).toFixed(1)},${y(r.grossSales).toFixed(1)}`).join(' '),cash=rows.map((r,i)=>`${x(i).toFixed(1)},${y(cashOf(r)).toFixed(1)}`).join(' '),salesArea=`${L},${T+plotH} ${sales} ${L+plotW},${T+plotH}`,cashArea=`${L},${T+plotH} ${cash} ${L+plotW},${T+plotH}`,grid=[0,.25,.5,.75,1].map(t=>{const gy=T+t*plotH,val=max-t*range;return `<line class="kk-grid-line" x1="${L}" y1="${gy}" x2="${L+plotW}" y2="${gy}"/><text class="kk-axis-label" x="${L-7}" y="${gy+3}" text-anchor="end">${Math.round(val/1000)}k</text>`}).join(''),step=Math.max(1,Math.ceil(rows.length/6)),labels=rows.map((r,i)=>i%step===0||i===rows.length-1?`<text class="kk-axis-label" x="${x(i)}" y="${H-10}" text-anchor="middle">${kkDateLabel(r.day)}</text>`:'').join(''),dots=rows.map((r,i)=>`<circle class="kk-dot-sales" cx="${x(i)}" cy="${y(r.grossSales)}" r="2.3"><title>${kkDateLabel(r.day)} · Satış ${kkMoney(r.grossSales)}</title></circle><circle class="kk-dot-cash" cx="${x(i)}" cy="${y(cashOf(r))}" r="2.3"><title>${kkDateLabel(r.day)} · Stopaj dahil bilinen nakit ${kkMoney(cashOf(r))}</title></circle>`).join('');host.innerHTML=`<svg viewBox="0 0 ${W} ${H}" role="img" aria-label="Günlük satış ve stopaj dahil bilinen nakit grafiği"><defs><linearGradient id="kkSalesArea" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#79c7ff" stop-opacity=".18"/><stop offset="1" stop-color="#79c7ff" stop-opacity="0"/></linearGradient><linearGradient id="kkCashArea" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#f2a65a" stop-opacity=".16"/><stop offset="1" stop-color="#f2a65a" stop-opacity="0"/></linearGradient></defs>${grid}<polygon class="kk-area-sales" points="${salesArea}"/><polygon class="kk-area-cash" points="${cashArea}"/><polyline class="kk-sales-line" points="${sales}"/><polyline class="kk-cash-line" points="${cash}"/>${dots}${labels}</svg>`}
function kkRenderFlow(data){const host=document.getElementById('kkFlowGrid');if(!host)return;const t=data?.totals||{},s=data?.settlementSnapshot||{},o=data?.otherFinancialSnapshot||{},final=Number(o.knownCashAfterFeesAndStoppage??t.knownCashAfterFeesAndStoppage??o.knownFeeNet??t.knownFeeNet??0),items=[['Toplam satış',Number(t.grossSales)||0,'base'],['İade',Math.abs(Number(t.grossReturns)||0),'negative'],['Komisyon',Math.abs(Number(t.commissionCost)||0),'negative'],['Hakediş düzeltmesi',Math.abs(Number(s.adjustmentNet??t.settlementAdjustmentNet)||0),'adjustment'],['Platform ücreti',Math.abs(Number(o.platformServiceFeeCost??t.platformServiceFeeCost)||0),'negative'],['Kargo',Math.abs(Number(o.cargoCost??t.cargoCost)||0),'negative'],['Stopaj',Math.abs(Number(o.stoppageNet??t.stoppageNet)||0),'negative'],['Bilinen nakit',final,'final']],max=Math.max(1,...items.map(x=>Math.abs(Number(x[1])||0)));host.innerHTML=items.map(([label,val,kind])=>`<div class="kk-flow-item ${kind}"><div class="kk-flow-bar-shell"><div class="kk-flow-bar" style="height:${Math.max(3,Math.round(Math.abs(Number(val)||0)/max*100))}%"></div></div><span title="${kkEsc(label)}">${kkEsc(label)}</span><strong>${kkMoney(val)}</strong></div>`).join('')}
function kkRenderDashboard(data){const unit=data?.unitSnapshot||{};kkVNext.evidence.sales=kkHasNumber(unit.salesEvidenceCoverage)?kkClamp(unit.salesEvidenceCoverage):null;kkVNext.evidence.returns=unit.returnEvidenceApplicable===false?null:kkHasNumber(unit.returnEvidenceCoverage)?kkClamp(unit.returnEvidenceCoverage):null;kkVNext.evidence.cost=kkHasNumber(data?.salesCostCoverage)?kkClamp(data.salesCostCoverage):kkHasNumber(data?.costCoverage)?kkClamp(data.costCoverage):null;kkRenderEvidence();kkRenderDaily(data);kkRenderFlow(data)}
function kkRenderRadar(data){const host=document.getElementById('kkLeakList');if(!host)return;const rows=Array.isArray(data?.moneyLeakRadar)?data.moneyLeakRadar:[];if(!rows.length){host.innerHTML='<div class="kk-live-empty">Belirgin veri boşluğu görünmüyor. Bu, muhasebe açısından kesin sorun olmadığı anlamına gelmez.</div>';return}host.innerHTML=rows.map(r=>`<div class="kk-live-row"><strong>${kkEsc(r.title)}</strong><span class="kk-status">${r.severity==='high'?'Öncelikli':'Kontrol et'}</span><span>${r.impactBasis==null?'Tutar belirtilmedi':`Etkilenen taban ${kkMoney(r.impactBasis)}`}</span><span title="${kkEsc(r.message)}">${kkEsc(r.message)}</span></div>`).join('')}
async function kkLoadDecision(){if(!activeConnectionId){kkVNext.decision=null;kkRenderEvidence();kkRenderRadar(null);return}try{const days=Number(els?.rangeDays?.value||30),data=await functionRequest('decision-center',{query:{connection_id:activeConnectionId,days}});kkVNext.decision=data;kkRenderEvidence();kkRenderRadar(data)}catch{kkVNext.decision=null;kkRenderEvidence();const host=document.getElementById('kkLeakList');if(host)host.innerHTML='<div class="kk-live-empty">Veri güveni ve radar şu an okunamadı.</div>'}}
async function kkLoadWebhookState(){const badge=document.getElementById('kkWebhookState'),btn=document.getElementById('kkWebhookBtn'),msg=document.getElementById('kkWebhookMessage');if(!badge||!btn||!msg)return;if(!activeConnectionId){badge.className='kk-live-state';badge.innerHTML='<i></i> Mağaza seç';btn.disabled=true;msg.textContent='Önce mağaza seç.';return}btn.disabled=false;try{const data=await functionRequest('webhook-manager',{query:{connection_id:activeConnectionId}});if(data?.active&&data?.providerVerified){badge.className='kk-live-state ready';badge.innerHTML='<i></i> Trendyol’da aktif';btn.textContent='Doğrula ve anahtarı yenile';msg.textContent='Webhook Trendyol tarafında ACTIVE olarak doğrulandı; finansal doğrulama ayrıca senkronla devam eder.'}else if(data?.configured){badge.className='kk-live-state warn';badge.innerHTML=`<i></i> ${kkEsc(data?.providerStatus||'Onarım gerekli')}`;btn.textContent='Canlı akışı onar';msg.textContent='Yerel kayıt var ancak sağlayıcı tarafı aktif/doğrulanmış değil.'}else{badge.className='kk-live-state warn';badge.innerHTML='<i></i> Kurulu değil';btn.textContent='Canlı akışı kur';msg.textContent='Kurulum, mağazanın kendi API bilgileriyle webhook aboneliğini açar.'}}catch(e){badge.className='kk-live-state';badge.innerHTML='<i></i> Doğrulanamadı';btn.textContent='Tekrar doğrula';btn.disabled=false;msg.textContent='Trendyol webhook durumu şu an doğrulanamadı; yerel kayıt aktif kabul edilmiyor.'}}
async function kkRegisterWebhook(){const btn=document.getElementById('kkWebhookBtn'),msg=document.getElementById('kkWebhookMessage');if(!btn||!msg||!activeConnectionId)return;btn.disabled=true;btn.textContent='Doğrulanıyor…';msg.textContent='Trendyol webhook durumu uzlaştırılıyor ve bağlantı anahtarı güvenle yenileniyor…';try{const data=await functionRequest('webhook-manager',{method:'POST',body:{connection_id:activeConnectionId}});msg.textContent=data?.active?'Canlı sipariş bağlantısı Trendyol tarafında doğrulandı.':'Bağlantı doğrulanamadı.';await Promise.allSettled([kkLoadWebhookState(),kkLoadLiveOrders(),kkLoadDecision()])}catch(e){msg.textContent=humanError(e)}finally{btn.disabled=false;if(btn.textContent==='Doğrulanıyor…')btn.textContent='Tekrar doğrula'}}
async function kkLoadLiveOrders(){const host=document.getElementById('kkLiveList');if(!host)return;if(!activeConnectionId){host.innerHTML='<div class="kk-live-empty">Canlı akış için mağaza seç.</div>';return}try{const data=await functionRequest('live-overview',{query:{connection_id:activeConnectionId}}),rows=Array.isArray(data?.liveOrders?.rows)?data.liveOrders.rows.slice(0,8):[];if(!rows.length){host.innerHTML='<div class="kk-live-empty">Henüz gerçek webhook sipariş sinyali yok.</div>';return}host.innerHTML=rows.map(r=>`<div class="kk-live-row"><strong>#${kkEsc(r.order_number||r.package_id||'—')}</strong><span class="kk-status">${kkEsc(r.status||'Bilinmiyor')}</span><span>${kkHasNumber(r.total_amount)?kkMoney(r.total_amount):'Tutar bekleniyor'}</span><span>${r.event_at?new Date(r.event_at).toLocaleTimeString('tr-TR',{hour:'2-digit',minute:'2-digit'}):'—'}</span></div>`).join('')}catch{host.innerHTML='<div class="kk-live-empty">Canlı siparişler şu an okunamadı.</div>'}}
function kkStartLivePolling(){if(kkVNext.liveTimer)clearInterval(kkVNext.liveTimer);kkVNext.liveTimer=setInterval(()=>{if(activeConnectionId&&!document.hidden)kkLoadLiveOrders().catch(()=>{})},12000)}
kkMount();kkRenderEvidence();kkStartLivePolling();if(typeof renderDashboard==='function'){const base=renderDashboard;renderDashboard=function(data){base(data);kkRenderDashboard(data)}}if(typeof alertRender==='function'){const base=alertRender;alertRender=function(data){base(data);const c=data?.financialTruth?.cargoAllocationCoverage;kkVNext.evidence.cargo=kkHasNumber(c)?kkClamp(Number(c)/100):null;kkRenderEvidence()}}if(typeof refreshConnectionData==='function'){const base=refreshConnectionData;refreshConnectionData=async function(){await base();await Promise.allSettled([kkLoadWebhookState(),kkLoadLiveOrders(),kkLoadDecision()])}}if(typeof resetDashboardOnly==='function'){const base=resetDashboardOnly;resetDashboardOnly=function(){base();kkVNext.evidence={sales:null,returns:null,cost:null,cargo:null};kkVNext.decision=null;kkRenderEvidence();kkRenderRadar(null);const chart=document.getElementById('kkDailyChart');if(chart)chart.innerHTML='<div class="kk-chart-empty">Veri bekleniyor.</div>';const flow=document.getElementById('kkFlowGrid');if(flow)flow.innerHTML='<div class="kk-chart-empty" style="grid-column:1/-1">Veri bekleniyor.</div>';kkLoadWebhookState().catch(()=>{});kkLoadLiveOrders().catch(()=>{})}}
