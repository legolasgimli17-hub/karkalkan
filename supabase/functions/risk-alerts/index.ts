import { createClient } from 'npm:@supabase/supabase-js@2.57.4'
const PROJECT_URL=Deno.env.get('SUPABASE_URL')||'',PROJECT_ORIGIN=(()=>{try{return new URL(PROJECT_URL).origin}catch{return ''}})(),fmt=new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Istanbul',year:'numeric',month:'2-digit',day:'2-digit'})
function allowedOrigin(o:string|null){if(!o)return true;if(o==='https://karkalkan.vercel.app'||o===PROJECT_ORIGIN)return true;try{const u=new URL(o);return u.protocol==='https:'&&u.hostname.endsWith('-krgzabdullah22-8562s-projects.vercel.app')}catch{return false}}function headers(o:string|null){const h:Record<string,string>={'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store, max-age=0','X-Content-Type-Options':'nosniff','Referrer-Policy':'no-referrer','Vary':'Origin'};if(o&&allowedOrigin(o)){h['Access-Control-Allow-Origin']=o;h['Access-Control-Allow-Headers']='authorization, apikey, content-type';h['Access-Control-Allow-Methods']='GET, OPTIONS'}return h}function json(s:number,b:unknown,o:string|null){return new Response(JSON.stringify(b),{status:s,headers:headers(o)})}function dayKey(date:Date){const p=fmt.formatToParts(date),g=(t:string)=>p.find(x=>x.type===t)?.value;return `${g('year')}-${g('month')}-${g('day')}`}function startDay(days:number){const p=fmt.formatToParts(new Date()),g=(t:string)=>Number(p.find(x=>x.type===t)?.value),mid=Date.UTC(g('year'),g('month')-1,g('day'))-3*60*60*1000;return dayKey(new Date(mid-(days-1)*86400000))}function n(v:unknown){const x=Number(v);return Number.isFinite(x)?x:0}function money(v:number){return Math.round(v*100)/100}function pct(v:number){return Math.round(v*100)/100}const THRESHOLDS={lowMarginPct:5,highReturnPct:20,highCargoBurdenPct:15,minSalesForRateAlert:500,minCargoAllocationCoveragePct:80}
Deno.serve(async(req:Request)=>{const origin=req.headers.get('Origin');if(!allowedOrigin(origin))return json(403,{error:'ORIGIN_NOT_ALLOWED'},origin);if(req.method==='OPTIONS')return new Response(null,{status:204,headers:headers(origin)});if(req.method!=='GET')return json(405,{error:'METHOD_NOT_ALLOWED'},origin);const auth=req.headers.get('Authorization')||'';if(!auth.startsWith('Bearer '))return json(401,{error:'UNAUTHORIZED'},origin);const key=JSON.parse(Deno.env.get('SUPABASE_PUBLISHABLE_KEYS')||'{}').default;if(!PROJECT_URL||!key)return json(503,{error:'SERVER_CONFIG'},origin);const sb=createClient(PROJECT_URL,key,{global:{headers:{Authorization:auth}},auth:{persistSession:false,autoRefreshToken:false}}),{data:ud,error:ue}=await sb.auth.getUser(auth.slice(7));if(ue||!ud?.user)return json(401,{error:'UNAUTHORIZED'},origin);const u=new URL(req.url),connectionId=u.searchParams.get('connection_id')||'',raw=Number(u.searchParams.get('days')||30),days=[7,30].includes(raw)?raw:30;if(!/^[0-9a-f-]{36}$/i.test(connectionId))return json(400,{error:'INVALID_CONNECTION'},origin);const {data:conn}=await sb.from('marketplace_connections').select('id').eq('id',connectionId).maybeSingle();if(!conn)return json(404,{error:'NOT_FOUND'},origin);const start=startDay(days),[{data:products,error:pe},{data:alloc,error:ae},{data:daily,error:de}]=await Promise.all([sb.from('marketplace_product_daily_metrics').select('external_product_id,sku,barcode,product_name,gross_sales,gross_returns,seller_revenue,estimated_profit').eq('connection_id',connectionId).gte('day',start),sb.from('marketplace_product_cargo_allocations').select('external_product_id,allocated_amount').eq('connection_id',connectionId).gte('invoice_day',start),sb.from('marketplace_daily_financials').select('seller_revenue,settlement_adjustment_net,platform_service_fee_cost,cargo_cost,stoppage_net').eq('connection_id',connectionId).gte('day',start)]);if(pe||ae||de)return json(500,{error:'DB_ERROR'},origin);const store=(daily||[]).reduce((a:any,r:any)=>{a.coreSellerRevenue+=n(r.seller_revenue);a.settlementAdjustmentNet+=n(r.settlement_adjustment_net);a.platformServiceFeeCost+=n(r.platform_service_fee_cost);a.cargoCost+=n(r.cargo_cost);a.stoppageNet+=n(r.stoppage_net);return a},{coreSellerRevenue:0,settlementAdjustmentNet:0,platformServiceFeeCost:0,cargoCost:0,stoppageNet:0});store.adjustedSellerRevenue=store.coreSellerRevenue+store.settlementAdjustmentNet;store.knownCashAfterFeesAndStoppage=store.adjustedSellerRevenue-store.platformServiceFeeCost-store.cargoCost-store.stoppageNet;const cargo=new Map<string,number>();let allocatedCargo=0;for(const r of alloc||[]){const id=String(r.external_product_id||''),amount=n(r.allocated_amount);allocatedCargo+=amount;if(id)cargo.set(id,(cargo.get(id)||0)+amount)}const allocationCoverage=store.cargoCost>0?Math.max(0,Math.min(1,allocatedCargo/store.cargoCost)):store.cargoCost===0?1:0,map=new Map<string,any>();for(const r of products||[]){const id=String(r.external_product_id||r.barcode||'').trim();if(!id)continue;const p=map.get(id)||{externalProductId:id,sku:r.sku||null,barcode:r.barcode||id,name:r.product_name||null,grossSales:0,grossReturns:0,sellerRevenue:0,profit:0,profitRows:0,rows:0};p.rows++;p.grossSales+=n(r.gross_sales);p.grossReturns+=n(r.gross_returns);p.sellerRevenue+=n(r.seller_revenue);if(r.estimated_profit!==null){p.profit+=n(r.estimated_profit);p.profitRows++}if(!p.name&&r.product_name)p.name=r.product_name;if(!p.sku&&r.sku)p.sku=r.sku;map.set(id,p)}const alerts:any[]=[];for(const p of map.values()){const cargoCost=cargo.get(p.externalProductId)||0,estimatedProfit=p.profitRows?p.profit:null,profitAfterCargo=estimatedProfit===null?null:estimatedProfit-cargoCost,marginAfterCargo=profitAfterCargo===null||p.sellerRevenue===0?null:profitAfterCargo/p.sellerRevenue*100,returnRate=p.grossSales>0?p.grossReturns/p.grossSales*100:0,cargoBurden=p.sellerRevenue>0?cargoCost/p.sellerRevenue*100:0,label=p.name||p.sku||p.barcode||p.externalProductId;if(profitAfterCargo!==null&&profitAfterCargo<0)alerts.push({type:'loss_after_cargo',severity:'critical',externalProductId:p.externalProductId,label,value:money(profitAfterCargo),message:`${label}: kargo dağıtımı sonrası tahmini katkı negatif.`});else if(marginAfterCargo!==null&&marginAfterCargo<THRESHOLDS.lowMarginPct)alerts.push({type:'low_margin',severity:'warning',externalProductId:p.externalProductId,label,value:pct(marginAfterCargo),message:`${label}: kargo sonrası marj %${pct(marginAfterCargo)}.`});if(p.grossSales>=THRESHOLDS.minSalesForRateAlert&&returnRate>=THRESHOLDS.highReturnPct)alerts.push({type:'high_return_rate',severity:'warning',externalProductId:p.externalProductId,label,value:pct(returnRate),message:`${label}: iade oranı %${pct(returnRate)}.`});if(p.sellerRevenue>0&&cargoBurden>=THRESHOLDS.highCargoBurdenPct)alerts.push({type:'high_cargo_burden',severity:'warning',externalProductId:p.externalProductId,label,value:pct(cargoBurden),message:`${label}: kargo yükü satıcı gelirinin %${pct(cargoBurden)}'si.`});if(estimatedProfit===null&&p.grossSales>=THRESHOLDS.minSalesForRateAlert)alerts.push({type:'missing_cost',severity:'info',externalProductId:p.externalProductId,label,value:money(p.grossSales),message:`${label}: satış var ama ürün maliyeti eksik.`})}if(store.cargoCost>0&&allocationCoverage*100<THRESHOLDS.minCargoAllocationCoveragePct)alerts.push({type:'cargo_allocation_gap',severity:'warning',externalProductId:null,label:'Kargo eşleştirme',value:pct(allocationCoverage*100),message:`Kargo maliyetinin yalnızca %${pct(allocationCoverage*100)} bölümü ürünlere kanıtlı dağıtılabildi.`});const order={critical:0,warning:1,info:2};alerts.sort((a,b)=>(order[a.severity as keyof typeof order]??9)-(order[b.severity as keyof typeof order]??9)||Math.abs(Number(b.value)||0)-Math.abs(Number(a.value)||0));const top=alerts.slice(0,20),counts={critical:alerts.filter(a=>a.severity==='critical').length,warning:alerts.filter(a=>a.severity==='warning').length,info:alerts.filter(a=>a.severity==='info').length};return json(200,{connectionId,rangeDays:days,startDay:start,thresholds:THRESHOLDS,counts,total:alerts.length,alerts:top,engine:'rules_v2',financialTruth:{coreSellerRevenue:money(store.coreSellerRevenue),settlementAdjustmentNet:money(store.settlementAdjustmentNet),adjustedSellerRevenue:money(store.adjustedSellerRevenue),platformServiceFeeCost:money(store.platformServiceFeeCost),cargoCost:money(store.cargoCost),stoppageNet:money(store.stoppageNet),knownCashAfterFeesAndStoppage:money(store.knownCashAfterFeesAndStoppage),allocatedCargoCost:money(allocatedCargo),cargoAllocationCoverage:pct(allocationCoverage*100)},disclaimer:'Kâr alarmları yalnız bilinen ürün maliyeti ve eşleştirilebilen kargo dağıtımı kapsamındadır. Stopaj nakit kesintisi olarak gösterilir; muhasebe net kârı değildir.'},origin)})
